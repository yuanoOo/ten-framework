{
    "params": {
        "region": "${env:AWS_ASR_REGION}",
        "access_key_id": "${env:AWS_ASR_ACCESS_KEY_ID}",
        "secret_access_key": "${env:AWS_ASR_SECRET_ACCESS_KEY}",
        "language_code": "en-US",
        "media_sample_rate_hz": 16000,
        "media_encoding": "pcm"
    }
}
