{
    "dump": true,
    "dump_path": "./tests/keep_dump_output/",
    "params": {
        "app_id": "${env:BYTEDANCE_TTS_APPID}",
        "token": "${env:BYTEDANCE_TTS_KEY}",
        "audio_params": {
            "sample_rate": 32000
        }
    }
}