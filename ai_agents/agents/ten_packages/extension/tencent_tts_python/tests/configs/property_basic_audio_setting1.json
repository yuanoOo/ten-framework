{
  "dump": true,
  "dump_path": "./tests/keep_dump_output/",
  "params": {
    "app_id": "${env:TENCENT_TTS_APP_ID}",
    "secret_id": "${env:TENCENT_TTS_SECRET_ID}",
    "secret_key": "${env:TENCENT_TTS_SECRET_KEY}",
    "sample_rate": 16000,
    "voice_type": "${env:TENCENT_TTS_VOICE_TYPE}"
  }
}